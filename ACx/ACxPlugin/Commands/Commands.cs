using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace ACxPlugin
{
	enum Command
	{
		AddParty,
		AP = AddParty,
		ClearSpells,
		CS = ClearSpells,
		EditConfig,
		EC = EditConfig,
		EditPolicy,
		EP = EditPolicy,
		Help,
		Level,
		L = Level,
		LevelSlow,
		LoginNext,
		LN = LoginNext,
		LoginNextLoop,
		LNL = LoginNextLoop,
		LoadParty,
		LP = LoadParty,
		LoadSpells,
		LS = LoadSpells,
		Log,
		PartyNearby,
		PN = PartyNearby,
		//Pickup,
		Plan,
		Policy,
		PrintLogins,
		SaveSpells,
		SS = SaveSpells,
		//T,
		//Test,

	}
}
